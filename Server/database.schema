CREATE TABLE users(
id INT AUTO INCREMENT,
email VARCHAR(254),
password VARCHAR(255),
firstname VARCHAR(255),
familyname VARCHAR(255),
gender VARCHAR(10),
city VARCHAR(255),
country VARCHAR(255),
PRIMARY KEY(email)
);